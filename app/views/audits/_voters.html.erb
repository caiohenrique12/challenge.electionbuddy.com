<table>
  <thead>
    <tr>
      <th>Changes</th>
      <th>User ID</th>
      <th>Created At</th>
      <th>Changed At</th>
    </tr>
  </thead>
  <tbody>
    <% voters.each do |audit_voter| %>
      <tr>
        <td><%= audit_voter.changed_data_info %></td>
        <td><%= audit_voter.user_email %></td>
        <td><%= audit_voter.created_at.strftime("%Y-%m-%d %H:%M:%S") %></td>
        <td><%= audit_voter.changed_at.strftime("%Y-%m-%d %H:%M:%S") %></td>
      </tr>
    <% end %>
  </tbody>
</table>